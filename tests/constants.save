#pragma once

#define EVOLVER_MAX_GENERATIONS 2 // -1 for indefinite
#define EVOLVER_POPULATION_SIZE 3
#define EVOLVER_RECORDS_PERIOD 1 // -1 for never

#define EVOLVER_MAFPO_RECORDS_PERIOD 1 // -1 for never

#define NUM_THREADS 2

#define DEBUG

#ifdef DEBUG
#define D(x) x
#else
#define D(x)
#endif
